import * as stylex from "@stylexjs/stylex";

export const cardTokens = stylex.defineVars({
  imageFilter: "grayscale(100%)",
  detailsIndicatorOpacity: "0",
  detailsIndicatorTransform: "translate3d(0, 0.5rem, 0)",
});
