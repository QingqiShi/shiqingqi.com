import type { ElementBase } from "../types";

// Leaf Element — verdant, gentle.
export const leafElement = {
  id: "leaf",
  label: { en: "Leaf", zh: "叶系" },
  accentColor: "#7eb86b",
} satisfies ElementBase;
