import type { ElementBase } from "../types";

// Ember Element — fiery, restless.
export const emberElement = {
  id: "ember",
  label: { en: "Ember", zh: "余烬系" },
  accentColor: "#e85d3a",
} satisfies ElementBase;
