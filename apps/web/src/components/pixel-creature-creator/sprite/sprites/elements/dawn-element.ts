import type { ElementBase } from "../types";

// Dawn Element — radiant morning warmth.
export const dawnElement = {
  id: "dawn",
  label: { en: "Dawn", zh: "黎明系" },
  accentColor: "#f08a72",
} satisfies ElementBase;
