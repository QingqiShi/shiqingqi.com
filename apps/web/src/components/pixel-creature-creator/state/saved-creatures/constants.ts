/** localStorage key holding every saved Creature. A frozen contract. */
export const PIXEL_CREATURE_CREATOR_SAVED_KEY = "pcc:saved:v1";

/** How many saved Creatures are kept before the oldest is evicted. */
export const PIXEL_CREATURE_CREATOR_MAX_SAVED = 10;
