// The tinker draws the same progressive blur the design system draws, so
// it reads the arithmetic from `@tuja/ui` itself.
export {
  buildBlurLayers,
  type BlurGeometry,
} from "@tuja/ui/components/build-blur-layers";
export { buildEdgeBlurLayers } from "@tuja/ui/components/build-edge-blur-layers";
